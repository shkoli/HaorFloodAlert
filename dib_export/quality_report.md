# Quality Report — dataset.csv

Generated by a pandas audit of `dib_export/dataset.csv` (the 77-event SAR-verified subset of `data/honest_training_data_v2.csv`).

## Shape
- Rows: 77
- Columns: 18

## Dtypes
| Column | Dtype |
|---|---|
| date | object |
| flood_label | int64 |
| source | object |
| VV | float64 |
| VH | float64 |
| vv_vh_ratio | float64 |
| rainfall | float64 |
| soil_moisture | float64 |
| temp | float64 |
| wind | float64 |
| slope | float64 |
| twi | float64 |
| forecast_rain_next_12h | float64 |
| ndwi | float64 |
| upstream_vv | float64 |
| forecast_rain_72h | float64 |
| data_quality | object |
| temp_anomaly | float64 |

## Missing values
No missing values in any column (0 / 77 for all 18 columns).

## Duplicate rows
0 fully duplicate rows.

## Date range
2014-12-10 to 2024-10-15. All 77 `date` values parse successfully as `YYYY-MM-DD`; no impossible or malformed dates found.

## Class balance
- Non-flood (`flood_label = 0`): 45 rows (58.4%)
- Flood (`flood_label = 1`): 32 rows (41.6%)

Moderately imbalanced but not severely so.

## Suspicious values / outlier check
- **Negative rainfall:** none found (`rainfall` min = 0.0, max = 266.8 mm) — no impossible values.
- **Negative forecast rainfall:** none found (both forecast columns min = 0.0).
- **slope and twi are constant** across all 77 rows (1.91 and 17.185 respectively) — expected, since these are static AOI-level terrain values, not per-event measurements; flagged here because zero-variance features carry no predictive information for a per-event model.
- **data_quality is constant** (`"real_sar"` for all rows) — expected, since this is the pre-filtered SAR-verified subset.
- **VV/VH backscatter ranges** (VV: -22.35 to -8.76 dB, VH: -29.24 to -15.23 dB) are within physically plausible Sentinel-1 GRD ranges for mixed land/water surfaces; no extreme outliers.
- **soil_moisture range** (14.2%–69.7%) and **temp range** (16.5–30.1°C) are physically plausible for the Sylhet/haor region; no negative or >100% soil moisture values.
- **wind** min = 0.0 km/h, max = 21.7 km/h — no negative values.
- **temp_anomaly range** (-1.93 to 3.06°C) is a reasonable deviation from monthly climatology; no extreme spikes.
- **No exact duplicate dates**: all 77 `date` values are unique.
- **source diversity**: 40 distinct source citations across 77 rows — no single source dominates disproportionately.

## Overall assessment
The dataset is clean: no missing values, no duplicate rows, no negative rainfall, no malformed dates, and no extreme outliers outside physically plausible ranges. The two constant columns (`slope`, `twi`) and the constant `data_quality` flag are artifacts of this being a static-terrain, pre-filtered subset rather than data-quality defects.
