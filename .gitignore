# Secrets — never commit
.env
alert_settings.json

# Claude Code local settings (not part of the public project)
.claude/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Large raw rasters / GEE exports (not needed to reproduce paper numbers)
*.tif
*.tiff
*.geotiff

# Runtime noise (generated locally, not part of the repo)
logger_output.txt
*.log

# Duplicate export / working assets (kept on disk, not part of the repo)
dib_export.zip
dib_export/
thesis_screenshots/

# Unrelated personal file swept in accidentally (kept on disk, not part of the repo)
Toady/
